sudo: false
language: node_js
node_js:
  - v18.0.0
cache: npm
branches:
  only:
    - main # build master branch only
script:
  - hexo generate
deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GH_TOKEN
  keep-history: true
  on:
    branch: main
  local-dir: public
